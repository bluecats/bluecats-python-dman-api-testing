# test_GET_beaconFirmware.tavern.3.yaml

---

test_name: GET Beacon Firmware 3 - BeaconFirmwareV3Controller

marks:
  - get

stages:
  - name: Return firmware Hex file by beacon identifier and firmware version on a local server.
    skip: True
    request:
      url: http://localhost:65069/beacons/{identifier}/firmware/{version}/Hex
      method: GET
      headers:
        content-type: application/json
        Authorization: "BlueCats {tavern.env_vars.SECRET_AUTH}"
        x-api-version: "3"
    response:
      status_code: 200