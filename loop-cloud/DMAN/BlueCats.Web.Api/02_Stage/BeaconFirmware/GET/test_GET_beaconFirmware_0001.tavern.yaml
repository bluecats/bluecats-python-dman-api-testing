# test_GET_beaconFirmware_0001.tavern.yaml

---

test_name: GET Beacon Firmware Test - 0001 - BeaconFirmwareV3Controller.cs (Stage Environment)

marks:
  - get
  - skip

stages:
  - name: Get firmware version by beacon model number.
    skip: True
    request:
      url: https://stage.api.bluecats.com/beaconmodels/{modelNumber}/firmware/{version}
      method: GET
      headers:
        content-type: application/json
        Authorization: "BlueCats {tavern.env_vars.SECRET_AUTH}"
        x-api-version: "3"
    response:
      status_code: 200