# test_CRUD_mfrbeacon.tavern.1.yaml

---

test_name: CRUD Mfr Beacon 1 - MfrBeaconsV1Controller

marks:
  - crud
  - skip 

stages:
  - name: Post, Get & Delete a mfr beacon with a JSON object running on a local server.
    skip: True
    request:
      url: https://stage.api.bluecats.com//mfr/beacons/{identifier}
      method: POST
      headers:
        content-type: application/json
        Authorization: "BlueCats {tavern.env_vars.SECRET_AUTH}"
        x-api-version: "3"
    response:
      status_code: 200