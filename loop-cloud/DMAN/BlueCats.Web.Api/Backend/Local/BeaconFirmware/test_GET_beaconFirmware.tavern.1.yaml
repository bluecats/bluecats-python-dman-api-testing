# test_GET_beaconFirmware.tavern.1.yaml

---

test_name: GET Beacon Firmware 1 - BeaconFirmwareV3Controller

marks:
  - get

stages:
  - name: Return firmware by beacon model number and firmware version on a local server.
    skip: True
    request:
      url: http://localhost:65069/beaconmodels/{modelNumber}/firmware/{version}
      method: GET
      headers:
        content-type: application/json
        Authorization: "BlueCats {tavern.env_vars.SECRET_AUTH}"
        x-api-version: "3"
    response:
      status_code: 200